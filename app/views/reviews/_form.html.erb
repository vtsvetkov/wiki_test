<%= form_for(review) do |f| %>
  <% if review.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(review.errors.count, "error") %> prohibited this review from being saved:</h2>

      <ul>
      <% review.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :rating %>
    <%= f.number_field :rating %>
  </div>

  <div class="field">
    <%= f.label :comment %>
    <%= f.text_area :comment %>
  </div>

  <div class="field">
    <%= f.label :review_pov %>
    <%= f.text_field :review_pov %>
  </div>

  <div class="field">
    <%= f.label :integration_type %>
    <%= f.text_field :integration_type %>
  </div>

  <div class="field">
    <%= f.label :integration_name %>
    <%= f.text_field :integration_name %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
