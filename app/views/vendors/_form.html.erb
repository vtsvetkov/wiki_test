<%= form_for(vendor) do |f| %>
  <% if vendor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(vendor.errors.count, "error") %> prohibited this vendor from being saved:</h2>

      <ul>
      <% vendor.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :vendor_name %>
    <%= f.text_field :vendor_name %>
  </div>

  <div class="field">
    <%= f.label :vendor_description %>
    <%= f.text_area :vendor_description %>
  </div>

  <div class="field">
    <%= f.label :vendor_rating %>
    <%= f.text_field :vendor_rating %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
